{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/psuthar/release-readiness-core/schemas/pr-gate-check-v1.schema.json",
  "title": "PR Gate Check Payload v1",
  "description": "Output of release-readiness-check-payload: shape consumed by actions/github-script for github.rest.checks.create / checks.update. Status mapping: PASS -> success, WARN -> configurable via --warn-conclusion (default action_required; alternatives failure for strict Phase 3, neutral for Phase 1/2 soft rollout), BLOCK / error -> failure. workflow_should_fail is the canonical signal for the enforcement step.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "check_name",
    "check_conclusion",
    "title",
    "summary",
    "text",
    "workflow_should_fail",
    "final_gate_status"
  ],
  "properties": {
    "check_name": {
      "type": "string",
      "description": "Name passed to github.rest.checks.create({name, ...}). Default 'release-readiness'; override via --check-name."
    },
    "check_conclusion": {
      "type": "string",
      "enum": ["success", "action_required", "failure", "neutral"],
      "description": "GitHub Checks API conclusion value. PASS always maps to 'success' and BLOCK/error to 'failure'. WARN maps to one of {action_required, failure, neutral} per the --warn-conclusion CLI flag (default action_required, which blocks mergeable_state but keeps the workflow green)."
    },
    "title": {
      "type": "string",
      "description": "Check output title (e.g. 'release-readiness: PASS')."
    },
    "summary": {
      "type": "string",
      "description": "Short human-readable summary line. May be empty."
    },
    "text": {
      "type": "string",
      "description": "Markdown body for the Check 'Details' page. Renders signals table, required actions, supporting detail, and footer."
    },
    "workflow_should_fail": {
      "type": "boolean",
      "description": "True when the calling workflow's enforcement step should exit non-zero. True for BLOCK and error payloads; false for PASS and WARN."
    },
    "details_url": {
      "type": ["string", "null"],
      "description": "Workflow run URL passed to checks.create({details_url}). Null when the CLI was invoked without --run-url and without $GITHUB_RUN_URL."
    },
    "final_gate_status": {
      "type": "string",
      "enum": ["PASS", "WARN", "BLOCK", "ERROR"],
      "description": "Mirrors final_gate.status from pr-gate-summary.json. ERROR indicates the input could not be parsed."
    }
  }
}
